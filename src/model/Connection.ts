export type Connection = {
    id: string,
    connectionName: string,
    databaseDriver: string,
    host: string,
    port: number,
    username: string,
    password: string,
    database: string,
    isActive: boolean
}