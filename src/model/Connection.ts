export type Connection = {
    id: string,
    connectionName: string,
    serverType: string,
    host: string,
    port: number,
    username: string,
    password: string,
    database: string,
    isActive: boolean
}